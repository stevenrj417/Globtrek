"use client";
import { useState } from "react";
export function UnsubscribeForm({ token }) { const [status, setStatus] = useState(""); async function unsubscribe() { setStatus("Updating…"); const response = await fetch("/api/email/unsubscribe", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ token }) }); setStatus(response.ok ? "You’re unsubscribed. No more marketing emails will be sent." : "We could not update your subscription right now."); } return <div><button type="button" onClick={unsubscribe} disabled={!token} className="min-h-14 bg-black px-7 text-[9px] uppercase tracking-[0.2em] text-white disabled:opacity-35">Unsubscribe</button><p role="status" className="mt-5 max-w-md text-sm font-light leading-6 text-black/55">{status}</p></div>; }
