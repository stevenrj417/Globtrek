import Link from "next/link";
import { UnsubscribeForm } from "../components/UnsubscribeForm";
export const metadata = { title: "Unsubscribe | Globtrek", robots: { index: false, follow: false } };
export default async function UnsubscribePage({ searchParams }) { const { token = "" } = await searchParams; return <main className="grid min-h-screen place-items-center bg-[#f3f0eb] px-6 text-[#171714]"><section className="w-full max-w-2xl border-y border-black/15 py-14"><Link href="/" className="text-[11px] font-semibold uppercase tracking-[0.3em]">Globtrēk</Link><p className="mt-16 text-[9px] uppercase tracking-[0.24em] text-black/40">Email preferences</p><h1 className="mt-4 font-serif text-5xl tracking-[-0.045em] sm:text-7xl">Leave quietly.</h1><p className="mb-10 mt-7 max-w-lg text-base font-light leading-7 text-black/55">One click stops future Globtrek marketing email. Your account and transactional trip emails are unaffected.</p><UnsubscribeForm token={typeof token === "string" ? token : ""} /></section></main>; }
